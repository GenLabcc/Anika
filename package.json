{
  "name": "anika-workspace",
  "private": true,
  "workspaces": [
    "my-app"
  ],
  "scripts": {
    "build": "CI=false npm run build --workspace=my-app",
    "start": "npm run start --workspace=my-app"
  }
}
